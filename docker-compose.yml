services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: tom_v3
      POSTGRES_USER: tom_v3
      POSTGRES_PASSWORD: tom_v3
    ports:
      - "5432:5432"
    volumes:
      - tom_v3_postgres:/var/lib/postgresql/data

volumes:
  tom_v3_postgres:
